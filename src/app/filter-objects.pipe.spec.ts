import { FilterObjectsPipe } from './filter-objects.pipe';

describe('FilterObjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterObjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
